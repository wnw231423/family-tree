# 族谱管理系统

## 快速启动

```bash
docker-compose up -d
```

启动后访问 http://localhost:5000

## 常用命令

```bash
# 停止
docker-compose down

# 停止并删除数据库数据（重新初始化）
docker-compose down -v

# 查看日志
docker-compose logs -f app
```
